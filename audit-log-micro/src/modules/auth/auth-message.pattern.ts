export const AuthMessagePattern = {
  login: 'auth.login',
  logout: 'auth.logout',
  tokenRevoke: 'auth.token.revoke',
  authorizeClaims: 'auth.authorize_claims',
  updatePassword: 'auth.update_password',
} as const;
