# ============================================
# Media Service Environment Configuration
# ============================================
# Copy this file to .env and update with your actual values
# cp example.env .env

# ============================================
# Application Configuration
# ============================================
APP_PORT=3002
APP_NAME=media-service
NODE_ENV=development

# ============================================
# Authentication & API Gateway
# ============================================
# Base URL of the API Gateway service
API_GATEWAY_BASE_URL=http://localhost:3000

# ============================================
# MySQL Database Configuration
# ============================================
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=your_mysql_password
MYSQL_DATABASE=trip_sync_media

# ============================================
# Google Cloud Storage (GCS) Configuration
# ============================================
# GCS Project ID
GCS_PROJECT_ID=your-gcp-project-id

# GCS Bucket Name (must be globally unique)
GCS_BUCKET_NAME=your-bucket-name

# Option 1: Use service account key file (recommended for local development)
# Path to the JSON key file downloaded from GCP Console
GCS_KEY_FILENAME=./path/to/service-account-key.json

# Option 2: Use embedded credentials (alternative approach)
# Service account email
# GCS_CLIENT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com
# Service account private key (replace \n with actual newlines when setting)
# GCS_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY\n-----END PRIVATE KEY-----\n"

# Note: Use either GCS_KEY_FILENAME OR (GCS_CLIENT_EMAIL + GCS_PRIVATE_KEY)
# If both are provided, GCS_KEY_FILENAME takes precedence

# ============================================
# NATS Message Broker Configuration
# ============================================
# NATS server URL(s) - comma separated for multiple servers
NATS_SERVER=nats://localhost:4222

# ============================================
# External Services
# ============================================
# Trip Service URL (optional, defaults to http://localhost:3003)
TRIP_SERVICE_URL=http://localhost:3003

# ============================================
# Logging Configuration
# ============================================
# Log level: debug, info, warn, error
LOG_LEVEL=info

